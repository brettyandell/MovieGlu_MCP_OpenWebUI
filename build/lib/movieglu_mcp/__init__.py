"""MovieGlu MCP.

Real theater showtimes, films, cinemas and ticket deep links from the
MovieGlu v200 API, exposed as MCP tools for Open WebUI chat sessions
(and any other MCP client).

Run:
    python -m movieglu_mcp                 # stdio transport
    python -m movieglu_mcp --transport http # Streamable HTTP (OpenWebUI)
"""

__version__ = "0.1.0"
